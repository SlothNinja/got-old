package main

import (
	"bitbucket.org/SlothNinja/user"
	"github.com/gin-gonic/gin"
)

func (g game) show(c *gin.Context) (game, error) {
	cu, _ := user.Current(c)
	g = g.updateClickablesFor(cu)

	return g, nil
}
